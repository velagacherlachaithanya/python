class Solution(object):
    def romanToInt(self, s):
        val = {'I':1,'V':5,'X':10,'L':50,'C':100,'D':500,'M':1000}
        res = 0
        for i in range(len(s)-1): res += -val[s[i]] if val[s[i]] < val[s[i+1]] else val[s[i]]
        return res + val[s[-1]]
